# Skip version bump if explicitly requested
if [ "$SKIP_VERSION_BUMP" = "1" ]; then
    exit 0
fi

npm run bump:patch

# Ensure the bumped files are included in the commit
git add package.json package-lock.json
