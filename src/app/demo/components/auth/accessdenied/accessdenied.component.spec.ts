import { AccessdeniedComponent } from './accessdenied.component';

describe('AccessdeniedComponent', () => {
    it('creates an instance', () => {
        expect(new AccessdeniedComponent()).toBeTruthy();
    });
});
